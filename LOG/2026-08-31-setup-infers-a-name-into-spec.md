# [HASH] — plan — the name-inference red flag cleared by design: setup never writes a name it was not given

**Red flag cleared — designed out, 2026-08-31.** The risk: setup wrote a name nobody supplied into the tester's SPEC — plausibly a colleague's, pulled from somewhere on his machine — the exact class the scrub checklist bans. The fix was agreed without waiting on the unsolved source question, because it does not depend on it: `docs/setup.md` gains the rule that no personal fact is written into SPEC or any scaffolded document unless the user supplied it in the interview's own answers, and any inference wording is removed. His instance is repaired by him — the message going out under [tester-data-carries-employer-material] step 3 asks where the string came from and points out the name is his to correct. The wider hole his own observation named — the spec-sync check tests whether SPEC agrees with what was built, never whether what is in it was ever true — split out as [spec-facts-unsourced], deliberately, being a far bigger design; its live option is that the never-infer rule plus builds-never-write-product-truth may close the entry routes and leave nothing for a checker to catch.

**Queue changes:** [setup-infers-a-name-into-spec] rewritten as the cleared build; [spec-facts-unsourced] filed as its own capture.
**Work processed:** kept — [setup-infers-a-name-into-spec].
