# d31b553 — Three rule-failure captures merged into one hand-over checkpoint

The advisory's three same-shape captures — [capability-just-granted-not-considered], [walkthrough-jargon-broken-by-its-own-author], [file-link-not-offered-at-hand-over] — weighed together as one finding: three shipped, clearly written rules each failed to fire while composing a hand-over message, and none while editing a file. Not recency, not wording; hand-over composition had no checkpoint.

The design amends the jargon rule's existing pre-send read-back into the single hand-over checkpoint, three questions in one pass: located terms and look-fors; a tool that could do the work instead (read from TOOLS.md, not memory); every named file linked. The other two rules stay canonical, cross-referenced. The honest limit is written into the item: this raises the odds of firing; nothing verifies the read-back ran. A rule per instance refused (admission gate); a fourth remember-to rule refused on the second capture's own evidence.

A fourth instance was folded in later the same session: the shared-vocabulary rule broken by this planning session itself ("standing note" minted as an alias for a capture, caught by the user's question) — again at composition, again not at a file edit. The checklist's first question gained the alias arm for it.

**Queue changes:** three captures deleted as merged; [handover-composition-checkpoint] written and cleared; the item later widened with the fourth instance.
**Work processed:** kept — [handover-composition-checkpoint]; deleted as merged — the three source captures.
