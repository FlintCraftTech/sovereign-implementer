# [HASH] — The capability-claim rule reaches what an outside surface permits, not just what a tool can do

A `[user]` walkthrough halted mid-drive on two assumptions about Discord nobody had checked, and both were load-bearing. The rule built the previous day covers a sentence asserting what a *tool* can do; a step asserting what an outside *surface* permits is the identical failure at a site that rule did not reach — and worse there, because a walkthrough is handed to a non-coder to perform with nobody to ask.

One sentence widens by one clause rather than a second rule being written beside it. The paragraph beneath names the walkthrough as where it bites hardest and points at the shape of the read: a throwaway of your own to act on, never anything of theirs.

The instance behind it closed the same day. The throwaway-topic test ran during this run's walk-through pass and settled both questions — a forum topic survives its opening message being deleted — which is the rule working on the case that produced it.

Tick: done, confirmed — the rule names outside surfaces as well as tools, and a grep for its distinctive words returns one statement rather than two.

Depth: short.

Rule gate: run — admitted as an amendment that widens one existing sentence. Parent named: the capability-claim rule itself. The alternative placement, in the `[user]` walkthrough requirements list, stays rejected: it would state one rule in two places and collide with [co-authoring-txt-draft-loop], which adds a bullet to that same list in this run.

**Files touched:** `plugin/throughliner/docs/skill-nonspecific-rules.md`.

**Routed to Captures:** none.
