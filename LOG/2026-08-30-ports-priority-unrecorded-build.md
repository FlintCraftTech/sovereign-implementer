# [HASH] — Ports being the top priority is written down, not carried by queue position

Filed by /rescan at the end of the 2026-08-29 planning session. The user's words that day: ports are now her number one priority. The four port items were placed at the top of the cleared region because of it, and that placement was the only trace.

Position is the wrong carrier, by this project's own reasoning. The queue is reordered on request, everything above the readiness line is built by one run so its internal order rarely matters, and the always-loaded rules say a relationship carried by placement rather than by text survives by luck. A reorder erases it with nobody noticing it was ever said.

The item's own prediction was confirmed the day it was processed: one planning session buried the four port items under a dozen newer cleared entries, so the placement signal eroded within twenty-four hours.

`CLAUDE.md`'s Current state section now carries one dated line. Dated deliberately — when the focus moves on, the line's age is visible rather than silently wrong, and it is one sentence to update on her word.

Two homes were refused and are recorded so they are not re-proposed. SPEC, because ports being supported is already product truth there and a priority is a current focus rather than a product fact. The memory project, because it ranks projects against each other, while this has to reach a fresh session opening *this* one.

**Observable met:** a grep finds the dated priority line in Current state.

**Depth:** short.
**Files touched:** `CLAUDE.md`.
**Routed to Captures:** none from this item.
