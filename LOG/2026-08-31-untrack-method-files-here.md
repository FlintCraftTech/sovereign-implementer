# [HASH] — plan — this project's own untracking filed as immediate work (sibling entry; the reasoning is at 2026-08-31-gitignore-choice-with-snapshots.md)

Her call to apply the decision here now rather than wait for the build — "enabling the gitignore is not build work in substance." The warning was given as one standalone turn and accepted on her "proceed": history stays public forever, no undo until the snapshot build ships, and the machinery reading these files' git history degrades from the untrack onward. The scope-lock correctly refused the `.gitignore` edit in this planning session (attempted so the refusal was real, not assumed), and she could not paste the lines herself on remote control, so the work filed as the first cleared build item — seven ignore lines plus `git rm --cached`, with the tester-data destination's ignore line folded in since both edit one file.

**Queue changes:** [untrack-method-files-here] filed and cleared first in the region.
**Work processed:** kept — [untrack-method-files-here].
