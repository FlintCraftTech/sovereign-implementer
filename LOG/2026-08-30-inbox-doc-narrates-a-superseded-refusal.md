# 077b8b9 — plan — the INBOX doc's self-history sentence evicted to the record

The sentence narrates a superseded refusal inside operative text, and the delete-and-read test already ran: what remains is a complete instruction, so what it carries is history. The build deletes it; the superseded reasoning survives in git history and in the removing build's own record, which is where the rationale split says it belongs. Two pre-cleared sentences bind the build as exclusions: the honest-limit statement and the live read-receipt prohibition both stay, because deleting either changes behaviour.

Rule gate: run — an eviction under the rationale split; no slot spent.

**Queue changes:** [inbox-doc-narrates-a-superseded-refusal] kept and cleared.
**Work processed:** kept — [inbox-doc-narrates-a-superseded-refusal].
