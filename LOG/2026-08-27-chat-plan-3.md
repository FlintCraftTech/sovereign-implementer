# [HASH] — Third session of 2026-08-27: release plan settled for the tested build (test2 = d31b553, out as v1.21.1), beta-channel item lifted, three captures filed

A short planning session cut over, at the user's direction, to preparing an off-cycle release. Yesterday's v1.21.0 release was of an untested build and was not running fine; the user's instruction was to release the version that has actually been tested and is running this session — 1.21.0-test2.

**The release plan, settled in discussion and to run after this close on the user's word "release".** The installed test2 build was identified **byte-for-byte as commit d31b553** (the second 2026-08-27 planning session's commit), by diffing the plugin cache against archived commits with line-ending noise stripped — the working tree has moved past it, carrying yesterday's 13-item build run, which is untested. So the release runs from d31b553, not from the folder as it stands: the zip is built from that commit, published as **v1.21.1** (a fresh release rather than rewriting the already-public v1.21.0 — Claude's recommendation, accepted), and the `beta` branch is pointed at d31b553 so testers install the tested code. Yesterday's build changes stay in git, unreleased until a rezip of them has been tested.

**The same problem was independently documented last night.** The inherited tail from yesterday's build session includes `resources/research/release-validation-chain-broken.md`, which records the same fact (test2 = d31b553) and three further defects in the release chain — validation always running on a build the tree has moved past, the stamp's line-ending fragility, and the stable-label selector having no label-to-commit mechanism. This session's hand re-derivation is live evidence of finding 1. Filed as [release-validation-chain-findings] so a planning session actually processes it.

**Confusions cleared in conversation, worth keeping because they will recur:** /done does not trigger a release and release is not a mode — it is a word said in ordinary chat, with /done first only so the release runs on a committed project.

**Queue changes:** the forward advisory read and cleared; [beta-tester-pathway] lifted and cleared to run — its blocker [weekly-release-cycle] shipped and was verified by running the installed parser; [howto-posts-bot-authorship] NOT lifted despite its blocker shipping — the bot cannot post in the how-to forum, so its hold was re-pointed at [bot-needs-howto-send-permission]; three captures appended at the close ([release-validation-chain-findings], [beta-tester-pathway-files-line-names-queue], [contest-by-number-ask-unexplained] — the last captured by the user, on the close's ownphrasing failing to say what "go" does).

**Work processed:** deleted [second-machine-marketplace-overwrite-check] — its premise was that the second machine might have held an earlier `flintcraft` marketplace registration the beta install silently overwrote; the user's answer, from memory, is that the machine never had one, so there was nothing to overwrite. Nothing relocated; the general hazard stays guarded in CLAUDE.md and the research file. The remaining 35 undated captures were not reached — the session cut to the release instead.

**Also in this chat:** the user asked to prioritise "the release issues"; one of the four release-relevant captures was processed before the cutover. Rule-gate dispositions listing: no refusals since the last planning session. The weekly release cycle is not due (v1.21.0 published 2026-08-26 satisfies this Wednesday); the release this session prepared is on-request, not the cycle's turn. The test-rezips entry for test2 is ready to draft (a plan and a build run have both exercised it) and was deliberately deferred for time. Claude wrote 2026-08-28 into the three capture texts by assumption and corrected all four instances to the clock's 2026-08-27 before commit.

Advisory: filed — release-validation-chain-findings

Routed to Captures: [release-validation-chain-findings], [beta-tester-pathway-files-line-names-queue], [contest-by-number-ask-unexplained]
