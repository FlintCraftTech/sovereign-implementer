# 077b8b9 — plan — terminal steps supply the commands that come before the run

Captured by the user from a live walkthrough in another of her projects, with a screenshot: a step needed a terminal "sitting in the project folder" and gave only the command to run — pasted as instructed it would have run from `C:\` and failed, because nothing supplied the `cd`. Her instruction: users of this method are assumed to need the steps that come before the run. The precondition was stated as a description rather than a step, so it read as done; the walkthrough owns the translation into typed commands. The walkthrough requirements gain the bullet: a terminal step supplies, as typed commands, whatever must be true for it to work — the `cd` with its actual path first — or states the command works from anywhere.

Rule gate: run — an amendment to the walkthrough requirements, one more subordinate bullet under the each-step-names-the-thing parent; no slot spent.

**Queue changes:** [terminal-steps-include-preconditions] filed, gated and cleared.
**Work processed:** kept — [terminal-steps-include-preconditions].
