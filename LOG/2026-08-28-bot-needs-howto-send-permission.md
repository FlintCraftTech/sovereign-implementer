# [HASH] — [user] done — how-to forum send permission granted and confirmed from the API

The user granted the bot's permissions in ❓how-to-throughliner before the walkthrough's first step was driven; per its own step 3, the grant was confirmed from the API rather than on report — effective permissions computed from guild roles plus channel overwrites: view, send/create posts, send in threads, create public threads all true. TOOLS.md's stale cannot-post sentence corrected in the same pass. The grant released [howto-posts-bot-authorship], lifted into the cleared region on Claude's recommendation and the user's agreement. One environment fact from the check landed in TOOLS.md: a raw Discord API call needs a `User-Agent` header or it 403s before auth.

**Outcome:** done — observable check passed. Removed from Processed at this close.
