# 2e9cb18 — Stop hook to ignore specimen-shaped slugs — the boundary derived from the docs' own convention

Surfaced by /rescan and processed the same session: discussing a specimen fix, the hook read placeholder slugs (`[slug-a]`, `[beta-slug]`) as claims of filed captures and blocked the turn — correct by its rules, cheap by its once-per-claim bound, and recurring since specimen discussion is ordinary planning work. Kept: slugs whose name contains "slug" as a word are ignored by the check — the shipped docs' own specimen vocabulary, not an invented list; no real slug in the queue's history contains the word, because a real slug names its work. Residual stated: a real item named `something-slug` would slip the check, which is now also a reason never to name one that way. Hook change; suites at the building close with a case for both shapes.

Rule gate: not needed — hook code, no method rule authored.
**Queue changes:** [stop-hook-placeholder-slugs] into Processed, cleared to run.
**Work processed:** kept — [stop-hook-placeholder-slugs].
