# [HASH] — plan — the beta-test material routed: the records audit held behind its download, the first-time question held behind the audit, the re-homing walkthrough rewritten

[beta-test-session-records] kept as an `[audit]`: read the tester's reconstruction (its nine observations, the `/clear` disaster flagged as strongest candidate) and the slim copies of whichever of his five sessions are actually Throughliner runs — identified first, the rest unread, his privacy and the reading cost pointing the same way — one capture per observation worth acting on, findings marked for the reconstruction's handoff-provenance footing. Held below the line behind [tester-data-carries-employer-material], whose walkthrough is what puts the files on disk. [setup-asks-if-first-time] skipped back to Unprocessed behind that audit on the gate's own logic: its two concrete failures are being fixed unconditionally (never-infer; the un-bundlable `/clear` instruction), and whether anything genuinely should fork on experience level is exactly what the audit's evidence will show — if every fix is unconditional, the right answer is no new question at all. [howto-posts-bot-authorship]'s walkthrough was rewritten whole to the new-topic design, merging the deleted [howto-rehoming-needs-new-topics]: the bot creates six new topics in reverse title order, she deletes the old topics whole, pins re-carried by hand, register re-pointed by grepping the old ids — and two old steps died with their reasons recorded (the survive-the-delete test moot under whole-topic deletion; the other-messages step moot since the type check proved every follow-up an automatic notice). [walkthrough-premise-read-content-not-type] was deleted on a gate refusal: the candidate clause is hindsight-shaped and the catch came from unprompted care, which is where a candidate stops.

**Queue changes:** [beta-test-session-records] kept `[audit]`, held; [setup-asks-if-first-time] held behind it as a capture; [howto-posts-bot-authorship] rewritten (130 words shorter, placement contradiction resolved); [howto-rehoming-needs-new-topics] merged and deleted; [walkthrough-premise-read-content-not-type] deleted on gate refusal.
**Work processed:** kept — [beta-test-session-records]; deleted — [howto-rehoming-needs-new-topics] (merged), [walkthrough-premise-read-content-not-type] (refused at the gate).
