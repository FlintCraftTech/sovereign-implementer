# 819f7f1 — The Cowork month is recorded where a session looks for the project's origin

The method was developed inside Cowork for about a month before this repository existed, which is what reconciles the user's "about four months old" with a first commit of 2026-06-01. That fact lived in one article's walk-through record and nowhere else — a poor home, since a session wanting the origin story reads `CLAUDE.md`'s plugin-history section, which began at the rebuild and said nothing about what came before.

The paragraph earns its place operationally rather than as biography: that section exists to stop a session chasing pre-rebuild commits, and this tells it where the earlier work went and what can still be checked. Two traces survive here — the orphan tags, and a single back-reference in a legacy log to a decision dated ten days before the first commit. The word "Cowork" appears in none of the legacy logs, so the record proves the work predates the repository without saying where it happened.

The proposed text was written as it stood, unchanged. The public-repository question was put to the user when the item was processed and answered by her giving the accurate version rather than withdrawing it.

The dated paraphrase correction the item also called for was already in `LOG/2026-08-28-law-prose-article-2.md`, written when the item was processed — checked rather than assumed, so nothing was owed there.

Tick: done, confirmed — the section names the Cowork period and both surviving traces.

Depth: short.

Rule gate: not needed — a history fact rather than a rule. The mechanical trigger fires because the commit touches `CLAUDE.md`, which is the case the "not needed" line exists to make visible.

**Files touched:** `CLAUDE.md`.

**Routed to Captures:** none.
