# [HASH] — plan — workshop/ becomes a method folder with resources/ inside it, migrated by /setup

The user's design, reached while settling what a repository visitor sees first: tracked means part of Throughliner, and working material lives in one folder every project gets — `workshop/`, with `resources/` inside it. Claude's objection that `resources/` was a shipped concept and not renameable was wrong and is recorded in the item so it is not re-raised: this is the project that authors the method, and /setup's migration carries every other project across. Her sequencing beat the alternative of a bespoke build doing the move: the migration does it, which also makes this project's own move the strongest possible test of the shipped machinery. Three items carry it — the method change (cleared), the rezip-restart-setup run (held on it, host-side), and the cleanup (held on that, runs alone).

Rule gate: run — admitted as an amendment re-homing the two-kinds rule's subject; no slot spent.

**Queue changes:** [workshop-becomes-a-method-folder] filed and cleared; [workshop-migration-setup-run] filed and held on it; SPEC gained the workshop paragraph at the decision step.
**Work processed:** kept — [workshop-becomes-a-method-folder], [workshop-migration-setup-run].
