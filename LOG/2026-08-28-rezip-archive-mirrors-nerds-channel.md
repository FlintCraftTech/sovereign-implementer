# [HASH] — Rezip archive reconceived: the rezip builds and archives its own zip, and a release ships the archived tested build

Processed from [release-validation-chain-findings] (deleted into its two work items; dispositions noted atop `resources/research/release-validation-chain-broken.md`). The reconception is the user's, in her words: the archive is *"literally a 15-item mirror of the rezips for nerds channel, each with its own readme that's synonymous with the post as it currently stands"* — a collection of rezips from the rezip ritual, not of releases. Release-from-the-archive is Claude's recommendation on her agreement: the release copies the picked build's archived zip, so "release the build we tested" becomes a file copy instead of the hand-derivation that stalled the v1.21.1 request. Closes findings 1, 2 and 4 of the research file; finding 3 went to [content-stamp-normalises-line-endings], whose entry cites this one. Claude's picks folded in, each reversible: the archive gitignored (rebuildable from `Commit:` lines, held on Discord), `plugin/release-zip-archive/` retired (GitHub Releases already keeps every release zip), readme sync riding the existing post-edit approval.

**Queue changes:** [rezip-archive-mirrors-nerds-channel] into Processed, cleared to run.
**Work processed:** kept — [rezip-archive-mirrors-nerds-channel]; deleted — [release-validation-chain-findings] (content fully relocated).
