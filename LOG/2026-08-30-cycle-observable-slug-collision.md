# [HASH] — plan — cycle observables must be distinguishable from planning's own records; method fix only, on the user's dogfooding rule

Found live in the first session to compute the defective observable: the claims-sweep cycle's "most recent LOG entry under this slug" read the cycle's own authoring record as a completed turn, and this session's opening falsely reported the sweep had run. The defect is the method's — /plan writes per-item records named by slug, and its cycle-authoring step lets an observable collide with them; any consumer inherits it. The user refused the local hotfix Claude offered alongside the method fix, and her ruling governs the item's shape: this project tests the method and waits for the fix to arrive the way a consumer would — the build amends the shipped authoring step and brings this project's one live definition into compliance as the amended rule's first application, not as a hand-edit.

Rule gate: run — an amendment to plan.md's cycle-authoring step; no slot spent.

**Queue changes:** [cycle-observable-slug-collision] filed and cleared.
**Work processed:** kept — [cycle-observable-slug-collision].
