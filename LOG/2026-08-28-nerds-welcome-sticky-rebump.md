# [HASH] — Bottom-pinning answered: the nerds welcome becomes a bot-authored sticky, held behind the archive rework

The user's problem — channels open at the bottom, so a top pin feels pointless — researched in-session: Discord has no native bottom-pin; the universal pattern is a sticky message a bot re-posts as the newest. Our bot has no daemon, but test-rezips traffic is mostly its own, so re-bumping at each entry post keeps the welcome at the bottom exactly when people look. Kept as two items per the mixed-work rule: the build (welcome text bot-authored from a repo source file, re-bump in the posting step, unchanged bytes covered by the entry's yes) held behind [rezip-archive-mirrors-nerds-channel], which is rewriting the same posting step; and the `[user]` retirement of her old pin, held behind the build. Refused: a third-party sticky bot (nothing needs one in a human-traffic channel yet) and a gateway daemon.

**Queue changes:** [nerds-welcome-sticky-rebump] and [nerds-old-pin-retired] into Processed, held below the line.
**Work processed:** kept (held) — both; [nerds-old-pin-retired]'s entry is this one, cited from its slug.
