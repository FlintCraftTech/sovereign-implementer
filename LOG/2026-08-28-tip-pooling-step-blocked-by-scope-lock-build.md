# [HASH] — Tip pooling ends: the rezip's candidate step files captures, and the seed file goes read-only

The item's own closing question decided it at processing, and the build carried it out: noticed candidates are filed as ordinary captures in Unprocessed — the one file every session may write — with the release marking cleared candidates by appending a line to each capture and /plan processing those. `ANNOUNCEMENT-IDEAS.md` reverts to a static seed archive, read for material and never written. The release ritual's rezip step now says in one clause why a pool file cannot work there: a rezip runs after a close, so the scope-lock classifies the session as planning and refused the write the first time the step ran.

Tick: done, confirmed — neither doc instructs a write to ANNOUNCEMENT-IDEAS.md; the only remaining mentions are the seed-archive description and the recorded reason the pool failed.
Files touched: CLAUDE.md, resources/release-ritual.md.
Rule gate: run — an amendment rerouting the step through the standing capture pathway (parent: the tip pipeline); the pooling instruction is the eviction.
