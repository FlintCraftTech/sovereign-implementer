# 077b8b9 — plan — the mid-line marker guard kept, widened to all three field markers

The keep-check the filing asked for was run at processing rather than assumed, and it strengthened the case: every reader of the three field lines anchors to line start, so `Blocked by:` and `Not before:` share the red flag's exposure — and a vanished hold releases held work early, worse than a missed flag. The lint gains one advisory check flagging any of the three literals appearing mid-line; widening every reader's pattern stays refused as the tolerate-at-the-reading-end move already taken twice. `Cycle:` joins the guard via [cycle-material-captures-still-ranked], which builds after this in the same file.

**Queue changes:** [red-flag-marker-silent-shape-failure] rewritten with the widened scope and cleared.
**Work processed:** kept — [red-flag-marker-silent-shape-failure].
