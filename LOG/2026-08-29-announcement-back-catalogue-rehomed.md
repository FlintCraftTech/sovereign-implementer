# [HASH] — plan — the announcements back catalogue moves under the bot, and the sweep doubles as a claims audit

**The user's decision**, given while processing the announcement-sweep coverage note: she is happy for her own posts to be deleted and replaced with Throughliner-project posts, so they stay editable as further falsifications occur. The back catalogue is short, so it is done once; after that, as corrections arise.

**Why a sweep rather than post by post.** A post cannot be re-posted without every claim in it being true of the shipped plugin at that moment — the existing posting rule. So re-homing all of them **is** a full verification pass over everything the channel has ever claimed, obtained as a side effect.

**The occasion that forced it.** Message `1540531465115410553` (2026-08-22) is public and both its claims are false: the generated build view was retired on the 27th, and the article it announces was superseded on the 23rd. `INBOX/sent.md` had recorded that post as "approved, not yet posted" — false since the day it was written, and the reason no repeal-grep ever fired on it. The register line and the sentence it had propagated into were corrected.

**Verified rather than assumed:** the bot can post to `#announcements` — the v1.21.1 announcement is already under its name. It cannot delete or edit her messages, which is why each step needs her.

**Every repost carries a dateline naming when it was originally posted.** Without it, twenty-odd reposts read as twenty-odd fresh announcements — the cost that made a back-catalogue sweep look wrong in the first place, and the thing that turns it into a visible archive rebuild.

**Queue changes:** [announcement-back-catalogue-rehomed] filed and cleared; [comparison-article-post-needs-rewrite]'s false sentence corrected; the register line corrected.
**Work processed:** kept — [announcement-back-catalogue-rehomed]. Settled — [tip-recycle-sweep-coverage-note], as a finding the close carries.
