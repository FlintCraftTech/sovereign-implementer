# [HASH] — Audit finding kept: done-plan's repeal block relocates to the host-side record

The disposition honours both readings of the delete-and-read test: the "repealed — do not reinstate" block leaves the shipped doc (consumers never author method steps), and its content moves to `resources/self-authoring-rules.md`, the recorded home of repeal histories — unless the build's grep finds the repeal's own LOG entry already carries it, in which case a citation suffices. Settled-stays-settled kept by relocation, not deletion. Rule gate: run — an eviction of shipped history, nothing added.

**Queue changes:** [ca-repeal-block-in-shipped-doc] into Processed, cleared to run.
**Work processed:** kept — [ca-repeal-block-in-shipped-doc].
