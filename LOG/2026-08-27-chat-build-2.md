# [HASH] — Chat-level record: a fifteen-item run, four walk-throughs, and five of this session's own rules broken by the session that wrote them

The work items have their own entries. This holds what belongs to no single one.

## Corrections the user gave

**The rationale-split draft, rejected on five counts and dropped.** Every count was that the post described an internal reorganisation in second-person benefit language: users "do not read throughliner rules"; there are "no current users so no one there to feel the difference"; the FAQ "was always supposed to be" where the user looks, so nothing changed for them; and twice, "why would anyone care about this?" The item was deleted on her direction — *"I say drop it."* Her one constructive note, that the interesting story is **why** the why-clauses were removed and its home is the law-prose article announcement, is captured. Full critique in that item's own entry.

**One post a day is per channel, not per server.** Her reasoning: the rule dates from when announcements was the only channel in use and everything went there. Given while directing the plain-English tip to go up on a day another post had already gone out. Filed rather than written, because admitting a rule into the method's text is the gate's business and the gate sits at planning.

**"There's no timing to this"** — the correction that became [criterion-not-restated-as-time], recorded here because it was given in conversation rather than against an item.

## Errors this session made and fixed

**A hand-over naming neither of two quotes.** The step said "the quoted install line" about a post containing two quote blocks; the user had to ask which, and whether the right thread was even meant. This was composed **in the same run** that shipped the three-question hand-over checkpoint, whose first question asks whether each step names the thing to click and the thing to look for. Filed as [handover-named-neither-of-two-quotes].

**A misread check output that nearly swallowed a real finding.** `rule_signals.py` printed two slugs under a heading saying a capture should be filed under each. One was read as already open and therefore satisfied. It was not open, and nothing mechanical would have caught it: the check goes quiet once an item carries its slug, so a session that wrongly believes one exists gets the same silence as one that filed it. Caught at the close's required re-run and filed as [live-rules-name-retired-terms].

**Two capture headings opening with an article**, both flagged by the queue lint and both re-headed. Small, and worth recording only because the lint caught what the author did not.

**A striation drawn off-canvas** by pasting pixel coordinates into millimetre path data. Recorded in `TOOLS.md` rather than only here, since the next vector edit is where it would recur.

## Decisions reached in conversation

**The bot's first real post went to #tips rather than announcements**, decided by applying the two-kinds rule built hours earlier in the same run: it explains one feature with no release behind it.

**A permission gap found by measurement rather than by trial.** The first send failed with a 403. Instead of discovering the rest one failed send at a time, the bot's effective permissions were computed for every channel from the guild roles plus each channel's overwrites — a read, never a test post. Reading and posting are separate grants, and the bot could already read the three channels it could not post to, which is exactly why nothing had surfaced the gap. The user granted tips and announcements; the how-to forum is still blocked and is filed as [bot-needs-howto-send-permission].

**A warning turn before posting on a day that already had a post.** The pacing rule would have held it; the user was told what it crossed and directed it up anyway, which is the rule working as designed rather than being overridden.

## What the four walk-throughs settled

All four ended **done** — none deferred, none unreached. Their records are `LOG/2026-08-27-beta-install-smoke-and-post-edit.md`, `-announcement-rationale-split-correction.md`, `-discord-post-plain-english-consent.md` and `-cycles-fixture-cleanup.md`. The beta install one is worth naming twice: it had been deferred at step 1 on 2026-08-26 with its central question — whether the pinned install post already named the tested ref — recorded as unanswerable from this side. The bot built earlier in this run answered it, and the answer was no, which made two already-posted Discord claims false until the user fixed the post.

## The pattern worth naming

Five of this session's own rules were broken by this session. The hand-over checkpoint, within the hour of shipping. The register-pointer rule was the fix for a fault this project had already written twice. The tips audit filed twelve candidates against criteria that missed the test the user supplied hours later. **This is the doubled-rules problem the compliance checklist describes, seen from the other side:** authoring a rule and following it are different acts, and nothing here tests the second. Recorded as an observation, not filed — one session is not a pattern, and this project bans speculative rules.

Wind-down re-scan: covered by the rescan just run. It filed five captures, and nothing happened between it and this close except the close's own work — so no second pass was made. (The stood-down arm was built in this same run and is not yet on the installed host; it is in force from the moment it was decided, as this project's standing rule requires.)

Rule checks: five run, one found something — two live references to retired mechanisms in hook code, filed under the slug the check prints.

Advisory: filed — [forward-advisory]

Rule gate: run — see each item's own entry; twelve dispositions were transcribed from the queue items rather than composed at the close.

Routed to Captures: [live-rules-name-retired-terms], [spec-owes-rescan-recommendation], [second-machine-marketplace-overwrite-check], [tip-candidates-need-visibility-screen], [bot-edit-path-unproven], [bot-needs-howto-send-permission], [cycles-fields-are-single-line], [one-post-a-day-is-per-channel], [law-prose-article-announcement-carries-the-why], [handover-named-neither-of-two-quotes], [claude-md-asserted-bot-posting-channels-unchecked]
