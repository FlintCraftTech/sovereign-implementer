# 077b8b9 — plan — the test-rezips readiness check gains its required Rezip entry: line, staying in CLAUDE.md

The open question settled at processing, and the counter-argument dissolved on inspection: the shipped close doc cannot hold a check consumers can never fire, and residence was not what failed — CLAUDE.md's other close obligations fire because each has a required artifact or mechanical trigger, and this check had neither. The line is the missing artifact: ready / not yet / none, one per qualifying close, so a skip becomes a visible gap. The honest limit carried as the shape always carries it: a line can be written dishonestly and nothing checks that; what this buys is that a missing line is visible.

Rule gate: run — an amendment to the test-rezips section, extending the required-artifact shape to one more subject; no slot spent.

**Queue changes:** [rezip-entry-check-has-no-artifact] kept and cleared.
**Work processed:** kept — [rezip-entry-check-has-no-artifact].
