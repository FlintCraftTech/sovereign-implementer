# d31b553 — Mermaid question held with the new capture bow-out field

Still unshaped and still waiting on the first real cycles doc, but the wait no longer costs a re-presentation every session: the capture gained `Blocked by: [weekly-release-cycle]` — the bow-out field shipped the day before, used here for exactly its designed case. It returns by itself when the cycles doc exists. The user later asked whether the enrichment would have happened had she skipped instead; the answer, from the rules: a word-skip touches nothing by design, and the enrichment provisions attach only to a recommended skip-to-defer — recorded because the distinction was worth her asking.

**Queue changes:** [cycles-mermaid-diagrams] held in Unprocessed with the blocker field.
**Work processed:** held — [cycles-mermaid-diagrams].
