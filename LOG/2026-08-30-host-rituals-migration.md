# 077b8b9 — plan — the rezip and release step lists become ritual definitions; push and the recovery prose stay put

Designed once both original blockers had built. The rezip's and release's numbered steps move into two ritual definitions in `CYCLES.md` — firing words "rezip" and "release", each with its `Writes:` field — and the weekly-release cycle points at the release definition instead of restating it. Push stays in CLAUDE.md untouched: it fires on a standing condition Claude must notice unprompted, not on a word, and the record already settled that such a rule cannot be fetched. The recovery procedures and marketplace-collision guard stay in `release-ritual.md` as the reference companion — what-if material, not steps of a turn. Held on the zip fix because both rewrite the same document and the migrated definition should carry the new Python zip step, not the PowerShell one it replaces.

Rule gate: run — an amendment relocating existing rules; nothing admitted, no slot spent.

**Queue changes:** [host-rituals-migration] designed, both stale blockers dropped, re-held on [zip-entries-use-backslash-separators].
**Work processed:** kept — [host-rituals-migration].
