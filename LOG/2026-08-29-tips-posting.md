# 7b751b6 — plan — seventeen tip items became one posting cycle's material instead of seventeen decisions

The ladder reached a tip candidate and the queue held **eighteen** items of the same shape — eleven from the features audit, five recycled from old announcements, a coverage note and one from a rezip. Presenting them one at a time would have been thirty-odd turns of exactly the tedium the user had named an hour earlier.

**What she asked for could not be given as asked, and saying so mattered.** The bulk route only ever deletes; moving entries into Processed stays one at a time, because sliding undesigned work into the ready region unread is the failure that ceiling exists to stop. So "settle all eighteen together" had to mean something other than approving eighteen keeps.

**The answer was that they are not eighteen decisions.** They are the material pool for one recurring activity the project's own rules already describe — candidates noticed at a rezip, cleared by a release, turned into posts at planning — with no cycle definition behind it. Offering a cycle is owed at recurring-shaped work, and this was it.

**[tips-posting] authored in `CYCLES.md` with the user present**: the tips channel, every three days on her declared cadence, observable the most recent tips line in `INBOX/sent.md`, eight steps for a turn. Its writable paths were checked rather than assumed — every path a turn touches is already permitted to a planning session, so it needs no declaration.

**Seventeen tips became material and none moved into Processed**, which is what keeps this inside the rule above. [tip-recycle-sweep-coverage-note] was deliberately excluded: it is a record, not a tip, and it carried a live problem of its own.

**Queue changes:** [tips-posting] cycle authored; seventeen tip captures reclassified as its material, none moved.
**Work processed:** kept as cycle material — seventeen `tip-*` captures. Filed — [cycle-material-captures-still-ranked].
