# 2e9cb18 — v1.21.1 released: the tested build (test2 = d31b553) published off-cycle, beta pointed at the same commit

An on-request release, run after the third 2026-08-27 session's close on the user's direction. No scheduling queue item exists — the weekly cycle's turn was already satisfied by v1.21.0 (published 2026-08-26) — so this record stands under its own slug. The user's constraint, stated at the ask: there is no week-old beta this early in the channel's life, so the release works with the test2 build — the reality of early running.

**What went out.** Tag `v1.21.1`, pre-release, on commit d31b553 (via the `beta` branch — the GitHub API refused the bare short hash as a target). The zip was packaged from a `git archive` export of d31b553 with only its version string set to 1.21.1 — 34 files, 277 KB, no bytecode — never from the working tree, which carries the untested 2026-08-27 build run. The old v1.21.0 zip was archived and the archive pruned to three. The `beta` branch was fast-forwarded 2a96ce4 → d31b553, so the tester install route serves the same code the release describes. The release notes supersede v1.21.0 and name v1.21.1 as the recommended install; the first draft framed v1.21.0's problems as a story and was rejected by the user — the published text states only what a reader needs.

**The host was deliberately NOT updated (ritual step 14 skipped).** The local marketplace sources the working folder, so `claude plugin update` would have installed the untested tree over the very build this release exists to pin. The installed 1.21.0-test2 is byte-identical to the released code, so the running host already is v1.21.1's content and no restart is needed.

**Ritual deviations, all from packaging an earlier commit:** the stamp guard's working-tree comparison was moot (the mismatch was the known, user-directed condition); the consistency sweep ran light — the docs shipped are d31b553's own, already swept at its session; the suites ran 27/27 against the working tree, with the released content's own validation being its rezip-time suites plus a full day of live sessions. The by-hand commit lookup this release needed is the root finding of `resources/research/release-validation-chain-broken.md`, queued as [release-validation-chain-findings].

**Tip candidates:** none cleared — the pool file carries no pooled candidates (the pooling step is blocked, [tip-pooling-step-blocked-by-scope-lock]); the twelve tip candidates live in the queue and are not release-gated.

Files: `plugin/throughliner.zip` (rebuilt from d31b553), `plugin/release-zip-archive/` (v1.21.0 archived in, v1.15.0 pruned), `plugin/throughliner/.claude-plugin/plugin.json` (1.21.0-test3 → 1.21.1), the release commit 743aa63, the `beta` branch, and the GitHub release v1.21.1.

## Post-release tail: the announcement posted to #announcements

Posted by the bot on the user's explicit yes, 2026-08-27, message id 1542476078835175527 — a second same-day post, made after one warning turn and the user's reaffirmation that the one-post-a-day rule should not exist (her position recorded on [one-post-a-day-is-per-channel]). Edited once the same day, on her instruction and yes: the link to the pinned "How to install" post folded into the third paragraph, replacing her own separate link message, which she deletes herself (the bot cannot). That edit was the bot's first genuine edit — the proof [bot-edit-path-unproven] was waiting for, recorded in TOOLS.md. The live text, verbatim as it stands after the edit:

> **Throughliner v1.21.1 is out — this is the build to be on.**
>
> v1.21.1 packages the build that's been validated in real use: full planning and build sessions were run on it before it was published. It supersedes v1.21.0, which shipped with issues — if you're on that one, update.
>
> Getting it is the same as ever: the install route in the pinned "How to install" post — https://discord.com/channels/1499591966592335892/1541661928554758184 — lands you on exactly this build, and if you already have Throughliner, ask Claude to update the plugin. The beta branch and the release page carry the same code, so whichever way you install, you get the same tested version.
>
> Still an early, testing-stage release — expect rough edges, and if something misbehaves, ask Claude to draft a GitHub issue on the Throughliner repository for you (it posts only on your yes).
