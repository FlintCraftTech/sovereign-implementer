# 4efdcff — Chat-level record for the 30-item build run of 2026-08-28

The largest run this project has made: 29 build items and one audit, worked top-down from a cleared region rebuilt the day before, plus both `[user]` walk-throughs addressed — one halted on findings, one deferred mid-processing by the user. Two INBOX deliveries triaged (five messages across the session), the weekly release computed not due at both ends of the session, and the rescan run before the close on the user's command.

**Also in this chat:**

- **The user corrected the record's own framing twice during the law-prose processing**, and both corrections travelled: the fifteen-year interest is Simply Sew's and belongs to the ADHD article, not this one; and she reviews drafts rather than authoring passages — wanting them as linked `.txt` files she can edit — which reshaped what [co-writing-flavour] is actually asking. The Cowork origin month (the method built inside Cowork for ~a month before this repository begins, because Claude Code felt too daunting) entered the record for the first time and was routed onward as [cowork-origin-month-unrecorded].
- **Her compaction observation landed as [compaction-has-a-designable-tell]**: /rescan's "undetectable from the inside" claim is too strong, since a session can cross-check the conversation against the durable artifacts a run leaves — with the asymmetry recorded that absence of artifacts is evidence and presence is not, so any check supports a warning and never an all-clear.
- **A mixed-up parallel session resolved harmlessly**: the one-warning-then-proceed turn was given for a same-project collision before she realised the other session was in the Throughliner *site* project.
- **Two new rules shipped this run were exercised in the run itself**: the two-part disposition ask carried the law-prose claim processing, and the capability-claim check held two unverified assertions (the `.txt` sidebar, forum-starter deletion) as unverified rather than acted on.
- The suites run at this close passed 27/27 after an interrupted first invocation; the rule checks' one finding was filed as [compliance-audit-lag] mid-run.

**Queue changes:** 30 items removed from Processed as built (one at each tick, by the queue tool); ~24 captures appended across the run, the rescan, the mail triage and this close.
**Routed to Captures:** [cross-project-research-citation], [retired-feature-leaves-orphan], [compliance-audit-lag], [co-writing-flavour], the five tip recycles and coverage note, [self-scoping-misses-observable-files], [walkthrough-asserts-unchecked-surface], [rendering-for-a-reader-away-from-the-files], [cowork-origin-month-unrecorded], [compaction-has-a-designable-tell], [augmentatism-article-material], [issue-check-foreign-repos], [sent-register-untracked] (red flag, uncleared), [method-defect-watch].
Rule gate: run — dispositions transcribed per item from the queue, none composed by the build; per-item lines in each entry. The session's own capture-filing authored no rule.
Wind-down re-scan: covered by the rescan just run, plus the mail triage and one user-raised capture landed after it; nothing further surfaced.
Advisory: filed — [forward-advisory].

This session ran across an interruption and resumed; all dates are 2026-08-28.

## After the close

**The 1.21.1-test1 rezip ran** — pushed (`4efdcff` to origin/main), version bumped, `__pycache__` swept, suites 27/27, cache pruned to two prior builds, installed via the CLI, stamps proved equal (`8c874952044d` both sides, the first stamps computed under the new line-ending normalisation), CLI at 2.1.220. Awaiting the user's full app restart.

**The new archive step was refused on its first ever run, and the refusal is the finding**: the scope-lock classifies a post-close session as planning, and `plugin/rezip-archive/` is on no writable list — the exact shape this run had just fixed for tip pooling, filed as [rezip-archive-blocked-by-scope-lock]. No archive entry exists for this build; it is rebuildable from `4efdcff`. The readme drafted for it, so the eventual entry does not recompose it:

> **v1.21.1-test1** — under testing; use at your own risk.
> Commit: 4efdcff
> Version: 1.21.1-test1
> The 30-item run's build: release-from-archive (this archive is its first artifact), the beta channel's two-event Wednesday turn, rituals in the cycles doc, the self-hosting seed at /setup, the FAQ-at-announcement unblock, seven compliance fixes across the shipped docs, the stop hook's placeholder carve-out, and the content stamp made line-ending blind.

**Tip-candidate check:** one filed, [tip-candidate-rituals] — rituals are the build's user-visible feature not already covered by a queued post item; the beta channel is owned by [beta-launch-announcement], and the rest of the build is internal under the visibility test.
