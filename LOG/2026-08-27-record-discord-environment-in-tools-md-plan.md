# d31b553 — TOOLS.md created; the Discord environment recorded, channel IDs left out

The overdue write done in-session: `TOOLS.md` created at the project root carrying the Discord facts learned live on 2026-08-27 — token path and header shape, readable channels, the per-channel 403 gotcha, the Message Content Intent trap, edit-own-only semantics — plus the `py`-not-`python` interpreter fact. The user's decision: channel IDs stay out of the committed file (the repository is public); a session looks them up through the bot in one call. The capture deleted with everything relocated; its two queue citations repointed at TOOLS.md.

Related the same session: [no-route-to-discord-is-false] was folded into [discord-posting-bot]'s build block (the amended CLAUDE.md text describes reading and posting separately, the approval rule restated as its own safeguard) and deleted.

Later the same session the token file was renamed `INBOX/.discord-bot-token.txt`: session_start's mail scan had surfaced the credential as unread mail and directed a full read into context every session. The leading dot rides the scan's existing dot-file skip — the address book's own convention — so no hook change and no rezip was needed; TOOLS.md and the bot item carry the new path. And TOOLS.md's "main has not been granted" assumption was corrected on the user's word: main is a Chagora testing channel, not a posting target.

**Queue changes:** [record-discord-environment-in-tools-md] and [no-route-to-discord-is-false] deleted after relocation; TOOLS.md created and twice amended.
**Work processed:** deleted after relocation — both captures.
