# [HASH] — Replies read built: the bot reads replies and mentions across every channel it can see, and the planning opening runs it

`discord_post.py` gained `replies --since YYYY-MM-DD` — a read with no send path, comparing message timestamps rather than anything stored, the issues check's posture. A channel the bot can list but not read history in is skipped rather than fatal, so one closed door cannot hide every other channel's replies. CLAUDE.md's bot paragraph gained the clause: the planning opening's correspondence check runs the replies read, anchored to the most recent planning record, one capture per reply carrying something new, sends only under the standing gate.

Tick: done, confirmed — run live against the real server: authenticated, paged the channels, reported no replies or mentions since 2026-08-20, which matches the intro post having drawn none.
Files touched: resources/discord_post.py, CLAUDE.md.
Rule gate: run — an amendment to the Discord posts section (parent named); nothing evicted; admission ground as processed — the gap made a miss unobservable. Host-only.
