# [HASH] — plan — the product-forward question kept as its own item, held on the inventory that has to run first

Reasoning in [2026-08-29-repo-inventory-audit](2026-08-29-repo-inventory-audit.md). Filed as a stub mid-session so [post-drafts-leave-the-queue] had something to resolve against, then reshaped at processing into the third of the three cleanup pieces.

**What it owns is the presentation question**, which needs the user's taste rather than a finding: the root holds seventeen tracked files with no separation between the product and the workshop — `README.md`, `INSTALL.md`, `LICENSE` and `SPEC.md` alongside `CLAUDE.md` (82KB), `QUEUE.md` (141KB), `ANNOUNCEMENT-IDEAS.md` (153KB), `FABLE-BRIEF.md` and three images.

**One fact found at processing changes what is worth doing:** `Throughliner-icon.png` is 4.7MB, roughly 40% of the working tree, and deleting it reclaims nothing — the blob stays in history, and this project refuses history rewrites because its records are full of commit hashes. So the icon is a question of where it sits, not of repository size.

**Held on the audit rather than designed now**, because what to foreground cannot be settled before knowing what each file is. Deciding twice is the failure that avoids.

**The chain is stated rather than discovered:** [post-drafts-leave-the-queue] waits on this, and this waits on the audit. Three deep, resolving rather than looping — slow, and shortening it means settling the drafts destination independently, which is the user's call.

**Queue changes:** [repo-cleanup-product-forward] filed, held below the line on [repo-inventory-audit].
**Work processed:** kept — [repo-cleanup-product-forward].
