# [HASH] — Kept items name the external facts their design rests on, and when each was checked

A candidate rule from the proactive-research-offers finding, ranked strongest there and gated in-session with the user present on 2026-08-30. The decision step's kept-item enumeration goes from five entries to six: an item now names the external facts the design rests on and, for each, when it was last verified.

The reason it earns a line rather than a note: an assumption written into an item reads exactly like a verified one and goes stale silently. Dated on the item, a premise that has aged is visible where the work is, rather than only when something fails against it. It would have reached the stale-research and environment-generalisation instances recorded in the risk-classes finding.

Site-bound, which is what both evidence bases say works. It lives in `plan.md`, firing at one step, rather than in the always-loaded rules — so it costs no slot in the file every session pays for.

The cost was stated at admission and is real: most kept items grow by about a line, paid at every keep.

It relates to [research-validity-criteria], built in the same run, without duplicating it: that governs how research is conducted so the assumption is right in the first place; this records what a design assumes at the moment it is kept.

**Observable met:** `plan.md`'s kept-item enumeration carries the sixth entry. SPEC's matching clause was written at the decision step ahead of the build, per the product-truth rule, and needed nothing here.

**Depth:** short.
**Files touched:** `plugin/throughliner/docs/plan.md`.
**Routed to Captures:** none from this item.
