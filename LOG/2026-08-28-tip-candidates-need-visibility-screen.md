# [HASH] — The twelve tip candidates screened for visibility: all pass, two borderline, the screen retired

The screen ran at processing rather than being kept as work: each candidate against "does this show up in a session the user is sitting in?", with the user's five recorded objections as the standard. Ten pass clearly; two are borderline and kept on stated grounds written into their entries — TOOLS.md (internal mechanism, but the file is visible and the failure felt) and the scrub-gate limit (invisible gate, but the tip answers a question users actually ask). The result mostly confirms because the audit was already filtering for features — the rejected rationale-split draft described internal arrangement, which none of these do. Each candidate now carries a screened line so no later session re-runs this; the open rule question went to [tip-test-wins-tiebreak] as its second amendment.

**Queue changes:** twelve tip candidates annotated in place.
**Work processed:** deleted — [tip-candidates-need-visibility-screen] (its work done, results on the candidates).
