# 077b8b9 — plan — the digest gains a capture-cites-cleared-work flag; the clean-pass ban survives the keep

The load-bearing question — detectable without judgment? — has a partial yes the recorded instance itself passes: the falsifying capture named the cleared item's slug, and the digest already extracts cited slugs; crossing the two facts is arithmetic. The flag joins the placement-contradictions block, prints only when found, and inherits the partial-coverage note: a capture that invalidates without naming the slug is not reached, said where it reports. The item's own provision binds the build unchanged: no clean pass may be reported, because a clean result would assert no cleared item is invalidated, which nothing can know.

Rule gate: not needed — a computed report joining an existing block; no rule text touched.

**Queue changes:** [newly-filed-work-invalidates-cleared-work] designed and cleared.
**Work processed:** kept — [newly-filed-work-invalidates-cleared-work].
