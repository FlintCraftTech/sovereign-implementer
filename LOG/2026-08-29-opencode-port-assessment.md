# fb5deef — Goal session: the Egnatia-OC OpenCode port assessed, and the findings sent as an injectable self-hosting queue

The user brought the throughliner-opencode repository (Egnatia-OC on GitHub) — a community port of Throughliner to OpenCode — and asked for a comprehensive assessment delivered in an unusual form: not a review document but a folder his own port can run on itself, so he develops it using the method. The session ran as approved-plan goal work under a freeform scope file, read-only against his code (no clone, nothing executed).

**What the assessment established.** The port is a tracking port done seriously: all 34 vendored files verified byte-identical to this repository's commit 743aa63 (v1.21.1) by local hash comparison against his manifest — the pristine-vendor claim holds. His pin is four commits behind, missing 1,796 insertions including six shipped artifacts that postdate his snapshot entirely (docs/ports.md, port_changelog.py, the self-hosting seed template among them) — so his own /setup cannot seed self-hosting, which is why the pack hand-delivered it. His shim spawns `python3` with no Windows fallback and fails open on every hook error, so the scope-lock can be silently off — sent as a red-flagged capture. Other findings read from his code: deny reasons may never reach the model (his ANALYSIS.md and his shim disagree), session-context injection rides an experimental OpenCode hook with no canary, his shim's stop-block cap can swallow a legitimate second block on top of stop.py's own per-claim limiting, OpenCode's first-match rules-file loading (AGENTS.md beats CLAUDE.md) can silently drop the method's project layer, and his CC BY-NC-SA license exactly matches this project's — compliant, filed as a positive so it isn't re-investigated.

**What was delivered.** A five-file folder, built untracked at the repo root, zipped with Python's zipfile (deliberately not Compress-Archive, per the open backslash-separator finding — he plausibly opens it on Linux), extracted and sent by the user over Discord DM, then deleted here on her word — she chose not to keep a copy, so this entry is the durable record. The five files: a cover note; a QUEUE.md of 13 findings as captures in canonical format, six marked bootstrap-critical, one red-flagged; a BOOTSTRAP.md answering the chicken-and-egg problem the user spotted mid-planning — a hand-run checklist proving five pillars (commands load, context injects, scope-lock denies with visible reason, lint and stop fire, /setup completes) before self-hosting is attempted, each failure pointing at a capture by slug; a CYCLES.md with an [upstream-catch-up] cycle whose turn looks for the port-facing changelog, named plainly as a coming-soon feature (the generator shipped in 819f7f1; no release has published one yet), with a diff-the-package fallback until it lands; and the shipped self-hosting seed adapted for OpenCode (rules-file first-match warning, host/target mapped to re-vendor plus plugin reload). Two DM texts were drafted for the user to send under her own account: the cover message, and a follow-up carrying a paste-ready capture suggesting he build his own mechanical rule-corpus checks — that layer was deliberately left out of the pack because rule_signals.py is hardwired to this repository's paths.

**Not-writing-his-SPEC was weighed and settled when the user asked:** his vendored setup's pre-existing-content path peeks, leaves the injected queue untouched, scaffolds LOG/ and FAQ/, and fills SPEC.md from his own interview answers — a SPEC written here would have asserted what his project is in his place. The register line in `INBOX/sent.md` carries the send with what it claimed; INBOX is gitignored, so the line lives outside this commit by design.

**Also in this chat:** plan mode's plan file was blocked by the scope-lock and worked around via scratchpad-plus-copy (captured); the advisory queue lint narrated after every Bash command (captured); the user moved the folder out mid-session and back once it was clear the setup question needed no folder access.

**Queue changes:** five captures appended to Unprocessed — [first-port-changelog-has-a-consumer], [ports-forum-gains-a-live-example], [scope-lock-blocks-harness-plan-file], [queue-lint-narrates-on-every-bash], and the due-turn capture [announced-claims-sweep]. Nothing processed, nothing reordered; the readiness line did not move.
**Work processed:** none — filing only; this was not a planning session.

Rule gate: not needed — no method text was touched; the AGENTS block sent outward is the shipped self-hosting seed adapted for another project's rules file, not an amendment to this project's rules.
Rezip entry: not yet — 1.21.1-test2, only this goal session has run on it; no full /plan or /next yet.
Advisory: not needed — the next work is the four port items already ordered at the top of the cleared region by the user's own priority, and this session adds nothing that would change what /next picks.

**Routed to Captures:** [first-port-changelog-has-a-consumer], [ports-forum-gains-a-live-example], [scope-lock-blocks-harness-plan-file], [queue-lint-narrates-on-every-bash], [announced-claims-sweep], [self-hosting-onboarding-research].

This session ran across 2026-08-29 and 2026-08-30. At the close the user reversed the earlier keep-no-copy call for the pack's text (her call both times): the five files are preserved verbatim in `resources/research/opencode-port-assessment-pack-2026-08-29.md`, with its index line, as the claims record behind the register line and the first data point of the self-hosting-onboarding research line ([self-hosting-onboarding-research]).
