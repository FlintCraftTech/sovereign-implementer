# [HASH] — Cycles parser to continue wrapped fields — removing the constraint beat documenting it

The item's own lean confirmed: `cycles_facts()` continues `Cadence:` and `Observable:` across wrapped lines (to the next field or blank line), so the next author writes natural prose and no session opening carries a silently truncated fact. A format note in every cycles doc would have guarded a limitation that can simply be deleted — the state-the-action principle. Placed before the rituals item, which extends the same parser. Hook change; suites at the building close, with a wrapped-field case.

Rule gate: not needed — hook code, no method rule authored.
**Queue changes:** [cycles-fields-are-single-line] into Processed, cleared to run, ahead of [ritual-definitions-and-offers].
**Work processed:** kept — [cycles-fields-are-single-line].
