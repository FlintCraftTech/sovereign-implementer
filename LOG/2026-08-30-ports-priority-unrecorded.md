# [HASH] — plan — the ports priority gets a dated line in CLAUDE.md's Current state

Of the three candidate homes, one fits: SPEC refused (a priority is a current focus, not product truth), the memory project refused (it ranks projects against each other; this must reach a fresh session opening this one), CLAUDE.md kept — the how-to-work-on-this document, read every opening. The item's prediction confirmed the day it was processed: this session's own moves buried the four port items under a dozen newer cleared entries, eroding the placement signal within a day. The line is dated so a moved-on focus shows its age instead of being silently wrong.

Rule gate: not needed — a dated orientation fact, no rule authored; recorded because CLAUDE.md is a gate-trigger path.

**Queue changes:** [ports-priority-unrecorded] kept and cleared.
**Work processed:** kept — [ports-priority-unrecorded].
