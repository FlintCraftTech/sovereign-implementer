# [HASH] — plan — the capability-claim rule widens to outside surfaces, and the halted walkthrough is repaired in place

The how-to re-homing walkthrough halted at its own step 3 on two assumptions about Discord nobody had checked: that each how-to topic is a single post, and that deleting the opening post leaves the topic standing. Reading the live forum disproved the first — six topics each hold two to five further messages. The second is still unsettled; a search found support threads pointing both ways.

**Two things were tangled here and they wanted different homes.**

**The general one is this item.** A rule already exists for a *document* asserting what a tool can do: run the read that would verify it, or write it as intended rather than as fact. A walkthrough step asserting what an outside *surface* permits is the same failure at a site that rule does not reach — and worse there, because a walkthrough is handed to a non-coder to perform with nobody to ask.

**Rule gate: run — an amendment widening one existing sentence.** The alternative placement was **rejected rather than overlooked**: putting it in the `[user]` walkthrough requirements list would state one rule in two places, and would collide with [co-authoring-txt-draft-loop], already adding a bullet there.

**The specific question was moved onto the item it actually blocks.** [howto-posts-bot-authorship] is cleared to run, so leaving its blocker filed beside it would let a run halt at the same step twice. Its walkthrough now opens with the safe bot-side test the finding already named, and step 2 puts the six-topics finding in front of the user as a decision.

**Amended again later the same session** from a screenshot: the extra messages are her edit notes, which makes deleting them the default; and the forum displays out of numeric order, so the same throwaway topic now also settles whether posting re-orders the list — needed *before* the sweep, since re-homing posts into every topic.

**Queue changes:** [walkthrough-asserts-unchecked-surface] kept and cleared; [howto-posts-bot-authorship]'s walkthrough rewritten to six steps.
**Work processed:** kept — [walkthrough-asserts-unchecked-surface]. Amended — [howto-posts-bot-authorship].
