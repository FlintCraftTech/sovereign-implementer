# 077b8b9 — plan — audit findings always route to the queue, and the actionable filter comes out

The user's ruling, from a live misroute the same morning: Claude read the always-loaded triage's finding arm over the audit procedure's findings-to-Unprocessed rule and recommended routing the repository inventory out of the queue. Her words: findings of audits always belong in the queue — it's work (planned writing) that doesn't. The second half is hers too: Claude does not decide whether a finding is actionable — the compile step's "one finding per actionable change" was a worth-it filter running silently before anything reached her, and it comes out; worth-doing is settled at /plan with her present. Kept: the dropped route for a finding Claude re-reads and finds factually wrong, already fenced from being a worth-it judgment.

Rule gate: run — two amendments to named parents (the triage's middle arm; the compile step's own sentence); no slot spent.

**Queue changes:** [audit-findings-always-queue] filed and cleared.
**Work processed:** kept — [audit-findings-always-queue].
