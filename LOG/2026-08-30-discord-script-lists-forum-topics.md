# [HASH] — plan — the working topic-listing function gets a threads subcommand

Exposure of an existing function, not new capability: `active_threads()` works live and nothing offers it on the command line, so sessions write throwaway scripts — the shape the tooling rules push away from. Two standing consumers: the claims sweep's topic-order check and the how-to re-homing's first step. Three cleared items now edit this script; queue order settles build order, each reading the file as it finds it.

**Queue changes:** [discord-script-lists-forum-topics] kept and cleared.
**Work processed:** kept — [discord-script-lists-forum-topics].
