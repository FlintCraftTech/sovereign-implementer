# [HASH] — YouTube/ joins .gitignore

One line, on the user's direction: the folder holds the channel's working material until it pops out as its own project.

Tick: done, confirmed — `git check-ignore` matches the new rule and `git status` no longer lists the folder.
Files touched: .gitignore.
Rule gate: not needed — a gitignore line, no method rule authored.
