# 2e9cb18 — Taskflow bridge re-dated to 2026-09-28; the scrub settled at the boundary

Taken up on its merits when its date passed: it cannot pass the buildability check while the file format it writes depends on Taskflow's designed-but-unbuilt export and additive import — scoping against another project's unshipped design would be guessing. Re-dated a month with the user's approval; the recorded lift judgment reads Taskflowapp's own LOG index for the export and additive import having shipped, rather than asking the user. The item's standing scrub question settled: a pushed task passes the same scrub checklist as any capture before crossing the boundary, with the standing limit stated (shapes and Claude's own read, nothing more).

**Queue changes:** [taskflow-personal-bridge] re-dated in Unprocessed, `Not before: 2026-09-28`.
**Work processed:** dated out — [taskflow-personal-bridge].
