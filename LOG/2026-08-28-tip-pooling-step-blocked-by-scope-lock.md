# [HASH] — Tip pooling ends: the rezip's noticing step files captures instead

The item's own closing question decided it — the audit filed twelve candidates straight into Unprocessed with nothing blocked, because the queue is the one file every session may write. The lock's refusal was correct; the siting was wrong. Kept: CLAUDE.md's tip-pipeline block and the rezip ritual's step reworded so noticed candidates file as ordinary captures, with `ANNOUNCEMENT-IDEAS.md` reverting to a static seed archive. Processed alongside the FAQ collision (this date's faq-writes entry) as the same structural family.

Rule gate: run — amendment rerouting the step through the standing capture pathway; the pooling instruction is the eviction.
**Queue changes:** [tip-pooling-step-blocked-by-scope-lock] into Processed, cleared to run.
**Work processed:** kept — [tip-pooling-step-blocked-by-scope-lock].
