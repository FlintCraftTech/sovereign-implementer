# [HASH] — The issue check reaches repositories this project does not own

The planning opening's issue check had two limbs — comments on issues the register records, and new issues on a repository the project owns — and nothing else was in view. Issues on other repositories, including the tool the method runs inside, bear directly on the work and no limb reached them.

Half the reported constraint is accepted and half is answered. Deriving *relevance* is a judgement and stays one, so no per-project declaration of outside repositories is built and the check does not pretend to filter mechanically. But the set does not need deriving — it needs narrowing, and the anchor the existing limbs already use does it: issues with activity since the most recent planning record turn a standing list into the few that moved.

The query was verified at processing rather than assumed, and it found something live on its first run: an open request that would make `.md` editable in the desktop viewer, which is the premise of another item cleared in this same run. Nothing in this project knew it existed.

Issue text is written by strangers, so the limb states plainly that it is read as data and summarised in the project's own words. And the bound is stated where it reports: it reaches issues the account is *involved in*, so one nobody here has touched stays invisible. The widening is real and bounded, and must not be described as covering everything that could bear on the project.

Tick: done, confirmed — the step names three limbs, the third carrying the query, the untrusted-text handling and both limits.

Depth: short.

Rule gate: run — admitted as an amendment. Parent named: the issue-check step in `plan.md`'s opening, which already has two limbs and an anchor; this is a third in that list.

**Files touched:** `plugin/throughliner/docs/plan.md`.

**Routed to Captures:** none.
