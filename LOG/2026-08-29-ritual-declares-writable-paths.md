# [HASH] — plan — rituals declare the paths their steps write, and the load-bearing unknown dissolved on inspection

Filed at the settlement of [rezip-archive-blocked-by-scope-lock] as the general fix for a failure that has now happened twice: a ritual step needing to write somewhere the session running it may not.

**The unknown that made it unbuildable turned out not to exist.** The item was filed saying the hook has no way to know which ritual is running, since a ritual is fired by a word and leaves no marker. But `plugin.json` is already permitted **unconditionally** — nobody gated it on a live rezip, and nothing has gone wrong. So the hook never needs to detect anything: it reads the project's `CYCLES.md`, takes the union of the paths its ritual definitions declare, and permits those.

**That also disposes of the bypass objection recorded beside the existing carve-out.** A declaration living in a committed file written at planning, with the user present, is not a session granting itself permission mid-run.

**The user's decision, with its cost put to her first: allow them all the time**, rather than only while the ritual runs. A declared path is then writable in any session — here one gitignored folder of build output, for a consumer whatever their own definition names.

**Rule gate: run — an amendment**, a fourth item in `plan.md`'s existing enumeration of what a ritual definition carries. Its eviction is conditional and written as such: the narrow carve-out comes out only once this has shipped **and** the rezip actually exists as a ritual definition.

It ships rather than being host-only, since consumers define rituals too — so a SPEC sentence was written with the user.

**Queue changes:** [ritual-declares-writable-paths] filed and cleared; ordering written onto [host-rituals-migration] as well as here.
**Work processed:** kept — [ritual-declares-writable-paths].
