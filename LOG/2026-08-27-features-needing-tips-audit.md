# [HASH] — Twelve longstanding features have never had a post: the tip-coverage sweep

Filed at the decision step from the user's instruction on the tips restructure: a load of longstanding features have never been posted about, and those are fed in by date rather than by release. This audit is what finds them.

**Method.** SPEC's feature set read against the three coverage sources — `INBOX/sent.md`, `FAQ/index.md` and `ANNOUNCEMENT-IDEAS.md` — one criterion held across the whole target rather than re-deciding per feature. Edited nothing, as the flavor requires.

**What is already covered, so it did not become a finding:** installing, `/setup`, the four commands' basics and `/rescan` are all covered by the five how-to forum posts; the unprocessed ordering ladder, the work-cycle loop, the beta channel and the session-start history check each have an announcement.

**Twelve findings, each filed as a capture marked unreviewed:** red-flag screening; the readiness line together with `Runs alone`; how work gets held by `Blocked by:` and `Not before:`; cycles; the cross-project INBOX; `TOOLS.md`; seeding the queue from SPEC; the format-epoch halt and the two softer currency checks; write-first approval with show-first on request; the `[freeform]` flavor; the scrub gate stated limit-first; and the forward-recommendation advisory.

**Two of those are worth naming for why they are uncomfortable rather than merely uncovered.** The scrub gate's tip is written limit-first — the honest answer that not publishing these artifacts is the only complete protection is worth more than the mechanism, and a user deciding whether to make a repository public will not get that from a tool that markets the gate. And the `[freeform]` finding exists because the "Running /next" post names `[audit]` and `[user]` and stops, so a user meeting a `[freeform]` halt has no way to tell a deliberate stop from a fault.

**The findings were filed before a test that would have changed some of them existed.** Later the same session the user rejected a drafted post on grounds neither the posting brief nor the new two-kinds test contains — that the thing described is invisible from where the user stands. Several of these twelve plausibly fail that. Filed as [tip-candidates-need-visibility-screen] rather than quietly re-scoping the audit's output after the fact.

Findings go straight to Unprocessed with no approval on the way in, so the next planning session is the first time anyone weighs them — against the user's own concern that the number of posts must not overwhelm the channel.

**Files:** none. An audit edits nothing.

Rule gate: not needed — an audit authors no rules.

Routed to Captures: [tip-red-flags], [tip-readiness-line], [tip-holding-work], [tip-cycles], [tip-cross-project-inbox], [tip-tools-md], [tip-seed-from-spec], [tip-keeping-projects-current], [tip-write-first-and-show-first], [tip-freeform-flavor], [tip-scrub-gate-limit], [tip-forward-advisory], [tip-candidates-need-visibility-screen]
