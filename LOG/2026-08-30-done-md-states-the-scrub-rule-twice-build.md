# [HASH] — The mis-sited second statement of the scrub rule is evicted from `done.md`

From the compliance audit of 2026-08-29, lens 1. Common reasoning for this run's five compliance fixes is in `2026-08-30-done-audit-records-a-repealed-approval-step-build.md`.

The rule was stated twice in one document, three sections apart. The first sat in a section about consolidating several checks into one narration — a section about *output shape*, which a filing rule is not. The second sits at the top of the entry-writing section, which is the moment the rule actually applies, and it carries what the first did not: why, and what to do with what is found.

So the first was both redundant and mis-sited, and the mis-siting is how the duplication got in unnoticed. It goes; the entry-writing statement stays untouched.

Small, and filed anyway because the class is what matters: two statements of one rule are optimal distractors for each other, which is the admission cost this project's whole gate is built on. A near-identical pair inside a single document is the cheapest possible instance of it to remove.

**Observable met:** a grep for the scrub-checklist pointer in `done.md` finds exactly one occurrence, at the entry-writing section.

**Depth:** short.
**Files touched:** `plugin/throughliner/docs/done.md` — shared with [done-mail-triage-missing-prompt-arm].
**Routed to Captures:** none from this item.
