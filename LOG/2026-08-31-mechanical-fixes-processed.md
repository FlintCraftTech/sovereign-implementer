# [HASH] — plan — nine defect captures from the 2026-08-30 run and the morning's mail processed into cleared fixes, one per decision

Each was its own turn; this entry carries them together because each disposition is short and its full reasoning sits on the item itself. [walkthrough-outcomes-miss-halted-and-partly-walked]: the outcome values restated — done, deferred, not reached, or what actually happened in one plain sentence — no fourth value, per the item's own warning; restatement over exception. [build-ticks-omit-the-confirmed-form]: next.md's specimen corrected to `done, confirmed` with next-build.md named as the wording's home — the eviction's show-don't-point form; the tool-shaped version went to the MCP umbrella. [observation-files-named-by-folder-stop-the-run]: the class fix — a hook-touching run is permitted the testing suites, since the close cannot commit a hook change until they pass; the paths-not-folders clause was refused at the gate as its only instances are now covered. [unbracketed-blocker-invisible] (Chagora's reproduced defect): two guards — a blocker resolving to nothing prints as a placement contradiction, and the lint flags the unbracketed form at write time. [queue-lint-narrates-on-every-bash]: one emission rule merging Chagora's count-only finding — the lint speaks only when the flag set changes against the last commit and names the change; silence means unchanged. [scope-lock-blocks-harness-plan-file]: the harness's plans directory permitted like the scratchpad, path resolved not hard-coded. [cycle-field-missing-from-line-format]: the `Cycle:` line added to the line-format fence, capture-only stated in the gloss. [held-item-is-its-own-verification] (AFK-cats' deadlock): an exception to the hold-back rule admitted — restatement attempted and it lost content — where the held item is the blocker's only verification, the hold is not written. [unpresented-item-content-relocated-early] (Chagora): content belonging to a not-yet-presented entry is carried to that entry's own turn; the report's other half earns nothing, existing rules unfollowed. [bare-command-name-fails-before-rules-load] (hers, with screenshot): session_start prints the qualified command names unconditionally and the workaround arm names the qualified retry — sidestepping the unverified why. [stop-hook-missed-an-unfiled-claim] kept as an `[audit]`: reproduce first, the fix filed only once the mechanism is named. [walkthrough-steps-two-actions-max] (hers): steps carry at most three instructions, checks and verifications counting exactly as actions do — the 3–5 working-memory research range at her chosen low end, replacing her earlier bare two, with both the derivation and the instructions-not-just-actions refinement supplied by her during the close.

**Queue changes:** twelve items cleared to run; two deletions recorded in their family entries.
**Work processed:** kept — all twelve named above.
Rule gate: run — each item carries its own disposition with parent named; one refusal (paths-not-folders) and one refusal recorded at the beta entry; the builds transcribe.
