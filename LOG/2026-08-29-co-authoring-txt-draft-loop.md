# [HASH] — plan — co-authored drafts go to an editable `.txt` with a read-back loop, and a tested finding is overturned

Split out of [co-writing-flavour] on the user's direction. The capture proposed processing a fourteen-claim list one claim at a time; she refused that outright — *"the interaction was long and probably too tedious, not time-saving"*, an experimental ask at the time — and gave the better diagnosis in its place.

**Her diagnosis is the item.** Co-authoring keeps getting shaped as Claude work, and the expensive part is her explaining an edit in chat when it is far easier to go in and edit the text herself.

**A tested record was overturned live.** `CLAUDE.md` states, as tested, that the desktop side panel opens `.md` read-only and `.txt` "not at all", which is why the Discord posting flow detours through Notepad. She checked: `.txt` **is** editable in the side panel and carries a save button. That correction rides the build, since a planning session may not write `CLAUDE.md`.

**The mechanism generalises a flow that already works for one narrow case.** Discord posts are already drafted to a file she edits directly; this makes that the shape for any walkthrough step where she edits text Claude drafted, with Claude reading the file back only on her word and looping until she says she is finished.

**Rule gate: run — admitted as an amendment**, a fourth subordinate requirement in the `[user]` walkthrough block, evicting `CLAUDE.md`'s Notepad sentence in the same move.

**A live external dependency was written onto it later the same session**, found by the query [issue-check-foreign-repos] proposes: `anthropics/claude-code#83476` asks for `.md` files to be editable in the desktop viewer, which is the premise of this item. Marked do-not-wait.

**Queue changes:** [co-authoring-txt-draft-loop] filed and cleared; SPEC sentence written.
**Work processed:** kept — [co-authoring-txt-draft-loop].
