# d31b553 — Tips split designed: two post kinds, the tip pipeline, and how-to posts under the bot

From the user's channel restructure ([discord-channel-purposes-split], deleted into this): some announcements posts are henceforth tips, announcements narrows to news, and old posts can recycle. Her pipeline design, carried into one build item: tip candidates noticed at the rezip and pooled in `ANNOUNCEMENT-IDEAS.md`; the release marks which candidates its shipped features clear; the next /plan files them as dated post items — new features first, historical tips on slow days. Tip staleness rides the existing repeal-grep once the register records each post's channel.

Her addition mid-processing: the how-to forum topics become bot-maintainable — each outbound post checks whether a how-to's claims are touched, a tweak is a bot edit under the approval rule, and the set stays small (welcoming purpose; a new one is her call). Bot maintainability requires bot authorship, so the one-time re-posting migration is [howto-posts-bot-authorship], held behind the bot script. The coverage sweep for longstanding features with no tip is [features-needing-tips-audit], an `[audit]` filing candidates only.

Rule gate: run — amendment to CLAUDE.md's Discord posts section; the "and nothing else" clause reworded to carry the tips kind; general-Claude-Code tips stay excluded; nothing else evicted.

**Queue changes:** [discord-channel-purposes-split] deleted into three items: [posting-rule-two-kinds-and-tip-pipeline] (cleared), [features-needing-tips-audit] (cleared, end-placed), [howto-posts-bot-authorship] (held below the line).
**Work processed:** kept — the three above; deleted — the source capture.
