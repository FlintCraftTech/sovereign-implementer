# 077b8b9 — plan — the owed environment facts written into TOOLS.md directly; the capture's premise was stale

The capture said TOOLS.md was not on the planning session's writable list; the list had since gained it — the scope-lock's own refusal message names TOOLS.md as writable. So the item resolved on the spot under the resolve-now rule: the Drive-reads-pptx fact, the `Throughliner expert` role, the showcase forum's definition, and the bot's known limits all written in, plus the folded-in sidebar fact from this session's rescan (the side panel cannot edit `settings.local.json`; walkthroughs name the Notepad route).

**Queue changes:** facts written into `TOOLS.md`; [tools-md-owes-three-facts] and [sidebar-cannot-edit-settings-json] deleted as fully relocated.
**Work processed:** deleted — both, relocated.
