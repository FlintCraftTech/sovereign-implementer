# [HASH] — FAQ template to join the planning writable list; three owed entries ride the build

Two standing laws in direct collision — the announcement-time FAQ rule and the planning scope-lock — recurring at every bot-posted announcement, twice in two days and a third time within hours of processing (the red-flags tip). The surgical fix: `pre_tool_use.py`'s planning list gains exactly the FAQ template pair (the `FAQ/` copies were already writable), and the same build writes the owed entries: the v1.21.1 update entry, the how-to-report entry, and the red-flags entry added when that tip posted. Both prior debt captures deleted into this item with their drafts folded in. Refused: the deferred-capture-per-announcement status quo (the FAQ lags forever by construction); making `FAQ/` canonical (the template ships).

Rule gate: run — a deliberate one-pair widening of a list kept narrow (parent: the scope-lock's standing list, in service of the FAQ-at-announcement rule), admitted on the recorded collisions; nothing evicted.
**Queue changes:** [faq-writes-at-announcement-unblocked] into Processed, cleared to run.
**Work processed:** kept — [faq-writes-at-announcement-unblocked]; deleted — [faq-entry-owed-release-announcement], [faq-entry-owed-issue-first-announcement] (folded in).
