# [HASH] — plan — post drafts leave the queue on principle, not to shorten it, and the LOG is ruled out as their home

The user's position, and the narrowing is hers: the captures are not in dispute — what does not belong in the queue is the full post text. A finished draft is a deliverable rather than work-reasoning, and the method's own render rule already says doc-resident text is pointed at rather than pasted.

**She ruled out the LOG, and her own doc routing agrees.** `LOG/` records what happened; a draft that has not gone out has not happened, so storing it there turns the record into a filing cabinet. Her words: *"log is not a place for writing to live."* Current practice is the other way — the 2026-08-22 draft's text sits in a session record — and that is evidence of practice, not of correctness.

**Measured, and it must not be sold as a size fix.** QUEUE.md is 772 lines; the verbatim draft text is **ten**, across three items. For the record of where the length actually comes from: roughly 155 of those lines arrived during this session as settlement prose.

**One contested point recorded as Claude's, since it is:** tracked rather than gitignored. An approved-but-unposted draft carries a public claim and cannot be reconstructed — the exposure cleared the same morning on [sent-register-untracked].

**The destination was deliberately not chosen**, because where it lands is a question about the repository's shape, which [repo-cleanup-product-forward] answers. That is also why this cannot yet state what it changes: the file list is one path short, which is why it is a capture rather than a work item.

**Queue changes:** [post-drafts-leave-the-queue] filed in Unprocessed, bowing out behind [repo-cleanup-product-forward].
**Work processed:** kept — [post-drafts-leave-the-queue].
