# d31b553 — Rezip entry redesigned by the user: posts at the close, sessions not time, zips attached

The capture's rezip-sited posting step was superseded by the user's own redesign at processing: an entry describes a build that has been exercised, so it posts at **/done**, ready when at least one full /plan and one full /next have run on the installed build since its rezip — sessions, not time; her correction on the point is recorded in [criterion-not-restated-as-time]. A fresh entry carries no usability rating; posting the new entry unlocks editing the previous one with a testing-outcomes summary and a rating out of 5 (her 3/5 for the current channel entry recorded; that first backfill is hers to paste, every later one a bot edit of its own message).

Her follow-up question — where do nerds get the build? — produced the zip decision: the posting step zips `plugin/throughliner/` as it stands and attaches it to the entry, making the channel name literally true; the zip is kept nowhere locally (Discord holds it; the commit line rebuilds any byte-for-byte), and on her instruction `plugin/zip-archive/` is renamed `plugin/release-zip-archive/` in the same build, grep-traced. A draft entry composed mid-processing was stood down when the redesign landed — nothing posted.

Placement host-only by construction: the check lives in CLAUDE.md, never the shipped close docs. Rule gate: run — amendment to CLAUDE.md's Discord section; the capture's own rezip-sited suggestion superseded on her redesign.

**Queue changes:** [rezip-posts-its-entry] rewritten whole and cleared; [rezip-name-promises-a-zip] deleted into it (defining sentence in the build; rename refused on the mismatch ground — docs saying "refresh" against a public channel saying "rezips" would mislead more).
**Work processed:** kept — [rezip-posts-its-entry]; deleted — [rezip-name-promises-a-zip].
