# [HASH] — plan — the compliance audit kept with its scope refreshed, and the refreshed list is not the one it was filed with

Filed by `rule_signals.py`'s audit-lag check, so nothing had weighed it before this turn — said plainly when it was introduced, since a script filed it and no person had looked.

**The item instructed a re-run rather than trusting its own snapshot, and the re-run changed the answer.** Three rule-bearing commits since the anchor record, covering `CLAUDE.md` and, under `docs/`, the whole done family plus `feedback-and-inbox.md`, `migrate-checklist.md` and `next-audit.md`. The original snapshot named `plan.md`, `next.md`, `rescan.md`, `setup.md` and `skill-nonspecific-rules.md` instead — it was computed mid-run, before that run's commit existed, so it listed files still being edited. The instruction to re-read the check on the day is kept, for the same reason.

**Placement is load-bearing rather than incidental**, and is written into the item as such: `[audit]` items go to the end of the cleared region anyway, and here that ordering is what makes the pass worth running — [digest-answers-whats-next] and [ritual-declares-writable-paths] both edit `plan.md`, so an audit running after them covers those changes instead of re-arming the check immediately.

The other four checks were clean at processing, and the growth report showed the always-loaded rules up 49 statements over thirty commits — a measurement with no threshold behind it, so not a finding.

**Queue changes:** [compliance-audit-lag] kept and cleared, at the end of the region with the other audits; delta scope rewritten.
**Work processed:** kept — [compliance-audit-lag].
