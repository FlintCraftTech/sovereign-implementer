# [HASH] — Cycles diagrams re-held behind the rituals build, not drawn and not dropped

Returned when its first blocker built; re-held on Claude's recommendation and the user's agreement, on two grounds written into the capture for the eventual design to answer: a diagram is a second copy of the steps nothing checks (the likely answer — authored and updated only by the build amending the definition), and the doc's shape is changing under it (rituals join the same doc). Not deleted: once the doc holds several definitions, a rendered diagram is cheap to read and genuinely orienting.

**Queue changes:** [cycles-mermaid-diagrams] re-held in Unprocessed, `Blocked by: [ritual-definitions-and-offers]`.
**Work processed:** re-held — [cycles-mermaid-diagrams].
