---
description: Pick the top queue entry and execute it — build or test, type-agnostic. One batch at a time, scope-locked.
---
# /next

The user wants to execute the next piece of work from the queue.

Plugin-wide behaviour rules at `docs/plugin-behaviour.md` govern this command at a level above the procedure below.

Read and follow the procedure at `docs/next.md`.
